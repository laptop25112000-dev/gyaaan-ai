[gyaan]
mixer_model = "vivek3.0"
search_provider = "demo"
model_provider = "demo"
max_sources = 3
deep_research_sources = 12
sources_per_research_query = 3

[roles]
enabled = ["reasoner", "summarizer", "source_checker", "coder", "researcher", "critic"]
